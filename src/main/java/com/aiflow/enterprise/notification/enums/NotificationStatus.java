package com.aiflow.enterprise.notification.enums;

public enum NotificationStatus {
    PENDING,
    QUEUED,
    SENDING,
    SENT,
    DELIVERED,
    PARTIALLY_DELIVERED,
    FAILED,
    RETRYING,
    EXPIRED,
    CANCELLED
}
